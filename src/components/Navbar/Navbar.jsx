import React from "react";

const Navbar = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "space-between",
        alignItems: "center",
        color: "white",
      }}>
      <strong style={{ margin: "10px" }}>Contact Us</strong>
    </div>
  );
};

export default Navbar;
